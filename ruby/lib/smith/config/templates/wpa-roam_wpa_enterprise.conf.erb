ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
network={
  ssid="<%= ssid %>"
  proto=RSN
  key_mgmt=WPA-EAP
  pairwise=CCMP
  eap=PEAP
  identity="<%= domain %>\<%= username %>"
  password="<%= password %>"
}
